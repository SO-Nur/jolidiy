<div class="p-4">
    <h4 class="text-secondary">Catégories</h4>
    <ol class="list-unstyled mb-0">
        {% for category in categories %}
            <li><a class="text-decoration-none" href="{{ path('/category/{slug}', { slug: category.slug }) }}">{{ category.name }}</a></li>
        {% endfor %}
    </ol>
</div>